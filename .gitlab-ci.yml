workstationTests:
  stage: test
  only: 
    - master
    - workstation
  script:
    - source $HOME/miniconda3/bin/activate
    - tox
    
clusterTests:
    stage: test
    only:
    - slurm
    - master
    script:
    - source $HOME/miniconda3/bin/activate
    - pip install --upgrade pip
    - pip install --upgrade tox
    - tox

